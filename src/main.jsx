import React from "react";
import { createRoot } from "react-dom/client";
import Baraja from "./Baraja.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Baraja />
  </React.StrictMode>
);
